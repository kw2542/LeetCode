Q:
  Implement strStr().
  Returns a pointer to the first occurrence of needle in haystack, or null if needle is not part of haystack.

Notes:
  The key is to consider all situations to terminate the loop.